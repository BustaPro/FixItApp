<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootServiceList"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F2F6FA">

    <!-- HEADER -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:background="@color/blueFixit"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp">

        <TextView
            android:id="@+id/tvTitulo"
            android:text="Servicios"
            android:textStyle="bold"
            android:textSize="22sp"
            android:textColor="#FFFFFF"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"/>

        <!-- NUEVO: Layout para nombre + bandera -->
        <LinearLayout
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingEnd="10dp">

            <!-- Bandera -->
            <ImageView
                android:id="@+id/imgFlag"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginEnd="6dp"
                android:src="@drawable/ic_launcher_foreground"
                android:background="@drawable/circle_white"
                android:padding="2dp"/>

            <!-- Nombre Usuario -->
            <TextView
                android:id="@+id/txtUserName"
                android:text="Usuario"
                android:textSize="14sp"
                android:textColor="#FFFFFF"
                android:textStyle="bold"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

        </LinearLayout>

        <Button
            android:id="@+id/btnLogout"
            android:text="Salir"
            android:textSize="12sp"
            android:backgroundTint="#FFFFFF"
            android:textColor="@color/blueFixit"
            android:layout_width="80dp"
            android:layout_height="32dp"/>
    </LinearLayout>

    <!-- BANNER -->
    <ImageView
        android:id="@+id/bannerServicios"
        android:layout_below="@id/header"
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:scaleType="centerCrop"
        android:src="@drawable/bg_fixit"
        android:alpha="0.45"
        android:layout_marginBottom="12dp" />

    <!-- TITULO INTERIOR -->
    <TextView
        android:id="@+id/tvBienvenida"
        android:layout_below="@id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tus servicios registrados"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold"
        android:padding="16dp"/>

    <!-- LISTA -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewServices"
        android:layout_below="@id/bannerServicios"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:clipToPadding="false"
        android:layout_marginBottom="70dp"
        android:scrollbars="vertical" />

    <!-- BOTÓN FLOTANTE -->
    <RelativeLayout
        android:id="@+id/btnAddService"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="20dp"
        android:background="@drawable/rounded_button"
        android:gravity="center">

        <TextView
            android:text="+"
            android:textSize="30sp"
            android:textColor="#FFFFFF"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

    </RelativeLayout>

</RelativeLayout>
